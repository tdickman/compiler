program test is
	global integer test;
	global integer test2;
	global string aString;
	global bool aBool;

	global procedure newProc(integer a in, bool b out)
	integer test;
	begin
		test := 1;
		return;
	end procedure;

	begin
		aString := "Testing...";
		test := (a * -b) + c*d + f;
		aString := "a";

		// if statement
		if ( true ) then
			test := false;
		end if;

		// for statement
		for (test2 := 0; test2<=10)
			test := test+1;
		end for;

		newProc(2, aBool);
		newProc(2);
		return;
	end program
